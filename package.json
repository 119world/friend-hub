{
  "name": "friend-hub",
  "private": true,
  "version": "1.0.0",
  "engines": {
    "node": "24.x",
    "npm": ">=10"
  },
  "scripts": {
    "install:all": "npm install --prefix frontend && npm install --prefix admin && npm install --prefix backend",
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix admin\" \"npm run dev --prefix backend\"",
    "build": "npm run build --prefix frontend && npm run build --prefix admin",
    "start": "npm start --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
